// JS for payments index page (add as needed)

// Placeholder function to prevent tree-shaking
function paymentsIndexInit() {
    // Initialize payments index functionality
    console.log('Payments index initialized');
}

// Make function globally available to prevent tree-shaking
window.paymentsIndexInit = paymentsIndexInit; 